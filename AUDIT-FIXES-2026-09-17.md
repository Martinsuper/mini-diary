# 检查问题修复与验收

## 已实现且有针对性验证

- 目录持久化和启动恢复；移动后重启读取原内容的 Electron 回归通过。
- 导入落盘事务、失败保留原内容与弹窗、成功同步富文本编辑器；失败注入和成功替换回归通过。
- 同日期导入跳过/替换/合并选择；空字段合并与混合文本格式处理。
- 所有导出格式提交草稿；立即导出测试通过。
- Markdown 使用 markdown-it，禁用用户原始 HTML；属性编码、混合表格、代码字面量和 MathML 测试通过。
- Mermaid 严格安全模式按需加载；图表实际 SVG 渲染回归通过。
- PDF 使用独立打印页面共享安全渲染，等待字体、图片和图表；PDF 文件生成回归通过。
- 加密快照轮转、恢复前校验密码、保留当前文件、设置恢复入口；服务测试和完整 UI 恢复回归通过。
- 搜索清空取消防抖；搜索结果不再静默截断，使用 100 条渐进展示，摘要复用纯文本索引。
- 自动主题实时同步；显式主题不受系统变化覆盖。
- 最后一个连续写作区间、未来日期、空日记和可见文字统计修复。
- 密码变更等待完成并显示结果，失败保留输入；解锁显示实际文件错误。
- 修改密码成功后才替换密钥；关键主进程日记操作串行化。
- 锁定清空搜索明文缓存；系统锁屏/睡眠触发锁定；重置明确等待顺序。
- 渲染进程失效或关闭无回执时提供明确退出选择，而非无限等待。
- 工具栏样式下拉宽度、图片独立入口、复制反馈、回到今天、搜索双层边框、长标题、窄屏可见工具栏、设置导航和危险操作样式。
- 提升次要文本和主按钮对比度，浅深代码高亮分色，原生控件 color-scheme/accent-color；主题颜色从同一 Sass 定义派生。
- radio 分组、格式/模式 aria-pressed、年月/帮助 Escape 行为、中文统计翻译。
- Sass 已从旧 @import/global map-get 迁移为 @use/map.get。
- Playwright 版本对齐，修正旧快照和错误选择器。

## 当前验证结果

- Jest：9 套、37 项通过。
- Electron UI：32 项通过（包含 8 项新增可靠性回归）。
- TypeScript、TS lint、CSS lint 通过。
- 生产构建成功且 Webpack 无警告；Mermaid/KaTeX 的按需分块仍会增加安装体积。
- 已查看浅色、深色及 500×600 窗口截图；窄窗口工具栏在视口内。

## 不能表述为已全部解决的项目

- 多年大数据、图片密集日记的性能压测及存储架构优化尚未完成。当前仍是整个日记 JSON 序列化、全量加密写盘；新增图片大小限制只能缓解，不能替代增量存储或 worker。
- Mermaid/数学排版引入了更多依赖与按需资源，构建体积警告仍在；尚未完成依赖精简和重复 KaTeX 资源优化。
- 高级 Markdown 仍需源码编辑，尚未提供表格/图片/公式的完整所见即所得编辑。
- 设置已有区域导航，部分新文案仍采用中英文选择而非完整的多语言词典；繁体及其他语言未完整验收。
- 锁屏/睡眠和崩溃回退已实现，但真实系统睡眠、渲染进程崩溃及慢磁盘的专项自动化验证尚不完整。
- Windows/Linux 安装包、签名发布、跨卷实际硬件迁移、屏幕阅读器与超长文档打印未实测。
- 数据文件写入已 fsync，目录项断电耐久性、权限边界和各种文件系统错误仍需更严格的故障矩阵测试。

以上限制必须与已通过的测试一起阅读。本次成果是主要缺陷修复及本机回归通过，不等同于所有长期优化和所有平台验收均已完成。
